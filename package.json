{
  "name": "spray-controller",
  "version": "1.0.0",
  "description": "Automated boom sprayer controller for precision agricultural application using one global flow-control model.",
  "main": "index.js",
  "scripts": {
    "prepare": "husky install",
    "lint:md": "markdownlint-cli2 \"**/*.md\"",
    "lint:md:fix": "markdownlint-cli2 --fix \"**/*.md\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.0",
    "markdownlint-cli2": "^0.18.1"
  },
  "lint-staged": {
    "*.md": [
      "markdownlint-cli2 --fix",
      "git add"
    ]
  }
}
