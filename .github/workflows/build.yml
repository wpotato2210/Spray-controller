name: Arduino CI

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - fqbn: "arduino:avr:nano"
            log_name: "arduino-avr-nano"
          - fqbn: "arduino:avr:uno"
            log_name: "arduino-avr-uno"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Arduino CLI
        uses: arduino/setup-arduino-cli@v2

      - name: Install AVR core
        run: |
          arduino-cli core update-index
          arduino-cli core install arduino:avr

      - name: Generate default config.h
        run: |
          mkdir -p include
          cat <<EOT > include/config.h
          #pragma once

          #define SECTION_COUNT 8         // Adjust as required
          #define FLOW_SENSOR_PIN 2
          #define PUMP_PWM_PIN 3
          // Add additional project-specific macros/constants here
          EOT

      - name: Prepare artifacts folder
        run: mkdir -p artifacts

      - name: Compile sketch
        env:
          FQBN: ${{ matrix.fqbn }}
          LOG_FILE: artifacts/${{ matrix.log_name }}.log
        run: |
          chmod +x ./ci/compile-sketch.sh
          ./ci/compile-sketch.sh | tee "$LOG_FILE"

      - name: Upload compiler logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: compile-log-${{ matrix.log_name }}
          path: artifacts/${{ matrix.log_name }}.log
